# 사용 설명서 — 직접 사이트 고치기

프로그램을 설치할 필요 없이 브라우저에서 다 됩니다. 코드는 볼 일이 없습니다.

사이트에 나가는 글은 **독일어와 영어**입니다(독일 관객용). 편집 화면만 한국어입니다.

## 관리 화면

**<https://www.boram-park.com/admin.html>** — 즐겨찾기에 넣어두세요.

여기에 편집하러 가는 버튼, 사이트 상태, 전체 페이지 목록이 모여 있습니다.
이 주소는 **사이트 메뉴 어디에도 없고** 검색에도 잡히지 않습니다. 주소를 아는 사람만
열 수 있지만, 고치려면 GitHub 로그인이 필요하니 남이 수정할 수는 없습니다.

## 처음 한 번만

1. [pagescms.org](https://pagescms.org) 에서 **Sign in with GitHub**.
2. `qbodrii-bit/Portfolio` 저장소 접근을 허용합니다.
3. 다음부터는 로그인만 하면 바로 들어갑니다.

## 고칠 수 있는 것

| 항목 | 내용 |
|---|---|
| **작품** | 제목, 연도, 재료, 매체, 작품 텍스트, 이미지 |
| **작가 / 약력** | 소개 글, 학력, 전시 이력, 이메일 |
| **홈 화면** | 표지 이미지와 이름 아래 문구 |

거의 모든 칸이 **(독일어)** 와 **(영어)** 두 벌입니다. 영어 칸은 사이트의 EN 화면에
쓰입니다. 비워두면 EN 화면에서 그 자리가 비어 보일 수 있으니 채우는 편이 좋습니다.

## 새 작품 추가

1. **작품 → Add entry**
2. 파일 이름을 정합니다. 소문자, 움라우트 없이, 띄어쓰기 대신 붙임표:
   `haeutung-reispapier` 처럼. **이게 곧 작품 페이지 주소가 되고 나중에 바꿀 수 없습니다.**
3. **같은 해 안에서의 순서** — 목록은 연도 내림차순(최신이 위)입니다. 이 숫자는 같은 해
   작품끼리만 비교하며 작을수록 위에 옵니다.
4. 칸을 채우고 **이미지 → +** 로 사진을 올립니다. 올린 순서가 작품 페이지에 나오는 순서입니다.
5. **Save**

캡션은 *Street Food* 처럼 사진마다 제목이 따로 있는 연작에서만 채우고, 나머지는 비워둡니다.

## 이미지 크기·정렬·간격

작품 편집 화면에서 바꿀 수 있습니다. **전부 비워두면 지금 모양 그대로**입니다.

| 칸 | 위치 | 내용 |
|---|---|---|
| **이미지 정렬** | 레이아웃 | 작품의 모든 이미지를 왼쪽 / 가운데 / 오른쪽에 놓습니다. 비우면 왼쪽 |
| **이미지 사이 간격 (px)** | 레이아웃 | 이미지끼리의 빈 공간. 비우면 16 (32 보통, 64 넓게, 96 아주 넓게) |
| **글과 첫 이미지 사이 간격 (px)** | 레이아웃 | 작품 텍스트 아래부터 첫 사진까지. 비우면 96 |
| **크기 (%)** | 이미지마다 | 본문 폭 대비 가로 비율. 100 = 꽉 차게, 50 = 절반 |
| **정렬** | 이미지마다 | 이 사진만 다르게 놓을 때. 비우면 레이아웃 설정을 따릅니다 |

예: 크기 50 + 가운데 → 가운데에 절반 크기로. 사진마다 왼쪽·오른쪽을 번갈아 주면 지그재그 배치가 됩니다.

휴대폰에서는 화면이 좁아서 **크기와 상관없이 사진이 가로 전체**를 쓰고,
64보다 큰 간격은 64로 줄어듭니다.

## 사진에 대해

카메라나 휴대폰에서 나온 원본을 그대로 올려도 됩니다. 저장하면 자동으로 크기를 줄이고
색을 sRGB로 바꿔서, 브라우저에서 원본과 같은 색으로 보이게 만듭니다.

올린 사진이 제대로 들어갔는지는 관리 화면의 작품 목록에서 **이미지 장수**로 확인할 수 있습니다.

## 사이트 구조

왼쪽이 메뉴입니다. **Work** 를 누르면 작품 목록이 펼쳐지고, 제목을 누르면 작품 페이지로
갑니다. 썸네일을 늘어놓은 목록 페이지는 없습니다 — 메뉴의 목록이 작품으로 가는 길입니다.

휴대폰에서는 같은 메뉴가 오른쪽 위 메뉴 표시 안에 들어갑니다.

## 저장한 다음

1~2분이면 사이트에 반영됩니다. 따로 할 일은 없습니다.

잘 됐는지는 관리 화면의 **사이트 상태**에서 봅니다. `passing`이면 정상이고,
`failing`이면 문제가 생긴 것이니 알려주세요.

## 건드리지 않는 게 좋은 것

- **`.html` 파일 전부** — 자동으로 만들어지며 다음 저장 때 덮어써집니다.
- **`scripts/` 폴더와 `content/source/` 폴더**

`content/source/`는 보관함입니다. 포트폴리오 원본 PDF와 고해상도 사진이 들어 있고,
사이트에는 쓰이지 않습니다.

## 아직 남은 것

- **영어 글**은 기계 번역이라 검수하지 않았습니다.
- **2026년 전시** `Momentaufhanme / Atelieraltesthonet` — 아마
  *Momentaufnahme, Atelier Altes Thonet* 의 오타로 보입니다. 확인 후 고쳐주세요.
- **영상 작업 3점**은 지금 정지 이미지만 있습니다. 실제 영상을 넣으려면
  Vimeo나 YouTube 링크가 필요합니다.
- 약력 페이지의 소개 글은 이력만으로 구성한 것입니다. 따로 써둔 작가 노트가 있으면
  그걸로 바꾸는 게 좋습니다.
